 Проектная работа "Веб-ларек"Стек: HTML, SCSS, TS, ViteСтруктура проекта:- src/ — исходные файлы роекта- src/components/ — папка с JS компонентами- src/components/base/ — папка с базовым кодомВажные файлы:- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды


или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код (base)

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.


#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Класс Server
Обеспечивает взаимодейтвие приложения с сервером.

Конструктор:
`constructor(api: IApi)` - В конструктор передается объект IApi.

Поля класса:
`protected api: IApi` - объект типа IApi. 

Методы класса:
`getProductList(): Promise<IProduct[]>` - делает get запрос на эндпоинт /product/ и возвращает объект, полученный от сервера, в котором находится массив товаров;
`createOrder(orderData: TOrderData): Promise<TOrderResponse>` - делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода, а возвращает объект, подтверждающий покупку на определенную сумму.



# Документация


## Данные

### Интерфейс товар IProduct
interface Product {
  id: string - идентификатор товара;
  title: string - наименование товара;
  image: string - ссылка на изображение товара;
  category: string - категория товара;
  price: number | null - стоимость товара;
  desciption: string - описание товара.
}

### Интерфейс покупатель ICustomer 
interface Buyer {
 payment: 'card' | 'cash' | '' - способ оплаты;
 address: string - адрес покупателя;
 email: string - электронная почта покупателя;
 phone: string - телефон покупателя.
}


## Слой моделей данных(Models)

### Класс ProductCatalog
Контролирует хранение товаров, которые можно купить в приложении.

Поля класса:
`private products: Product[] = []` - массив всех товаров;
`private selectProduct: Product | null = null` - товар, выбранный для подробного отображения.

Методы класса:
`saveProducts(productsArray: Product[]): void` - сохранение массива товаров полученного в параметрах метода;
`getProducts(): Product[]` - получение массива товаров из модели;
`getProductById(id: string): Product | undefined` - получение одного товара по его id;
`setSelectedProduct(product: Product): void` - сохранение товара для подробного отображения;
`getSelectedProduct(): Product | undefined` - получение товара для подробного отображения.


### Класс BasketModel
Обеспечивает хранение товаров, которые пользователь выбрал для покупки.

Поля класса:
`private items: Product[] = []` - массив товаров, выбранных покупателем для покупки.

Методы класса:
`getItems(): Product[]` - получение массива товаров, которые находятся в корзине;
`addItem(product: Product): void` - добавление товара, который был получен в параметре, в массив корзины;
`removeItem(product: Product): void` - удаление товара, полученного в параметре из массива корзины;
`clearBasket(): void` - очистка корзины;
`getTotalCost(): number` - получение стоимости всех товаров в корзине;
`getItemCount(): number` - получение количества товаров в корзине;
`hasItem(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода. 


### Класс Customer
Обрабатывает данные покупателя, которые тот должен указать при оформлении заказа.

Поля класса:
`private payment: 'card' | 'cash' | '' = ''` - вид оплаты;
`private address: string = ''` - адреc;
`private phone: string = ''` - телефон;
`private email: string = ''` - email.

Методы класса:
`savePayment(method: string): void` - сохранение данных вида оплаты;
`saveAddress(address: string): void` - сохранение данных об адресе;
`savePhone(phone: string): void` - сохранение данных о телефоне;
`saveEmail(email: string): void` - сохранение данных о email;
`getCustomerData()` - получение всех данных покупателя;
`clearCustomerData(): void` - очистка данных покупателя;
`validateData()` - валидация данных.



## Слой представления(View)

### Класс Header extends Component<IHeader>
Обеспечивает отображение шапки сайта.

Поля класса:
`counterElement: HTMLElement` - счетчик товаров.
`basketButton: HTMLButtonElement` - кнопка счетчика товаров;

Методы класса: 
`set counter(value: number)` - установка счетчика товаров.


### Класс Gallery extends Component<IGallery>
Обеспечивает отображение каталога с карточками товаров.

Поля класса:
`catalogElement: HTMLElement` - каталог товаров.

Методы класса: 
`set catalog(items: HTMLElement[])` - установка каталога товаров.


### Класс Modal extends Component<IModal>
Обеспечивает отображение модального окна.

Поля класса:
`closeButton: HTMLButtonElement` - кнопка закрытия модального окна;
`modalContent: HTMLElement` - контент модального окна. 
`protected openFlag: boolean = false` - состояние модального окна: открыто или закрыто

Методы класса: 
`open()` - отвечает за открытие модального окна.
`close()` - отвечает за закрытие модального окна.
`isOpen(): boolean` - возвращает значение openFlag.
`set modal(content: HTMLElement)` - установка контента модального окна.


### Класс Sucess extends Component<ISucess>
Описывает карточку, появляющуюся при успешном совершении заказа.

Поля класса:
`closeButton: HTMLButtonElement` -  хранит кнопку для закрытия контейнера и перехода к новым покупкам.
`totalElement: HTMLElement` - хранит общую стоимость купленных товаров.
 
Методы класса: 
`set total(value: number)` -  метод для указания общей стоимости товаров.


### Класс CardCatalog implements Component<ICardCatalog>
Обеспечивает отображение карточки в каталоге товаров на главной странице.

Поля класса:
`imageElement: HTMLImageElement` - хранит изображение.
`categoryElement: HTMLElement` - хранит категорию товара.
 
Методы класса: 
`set image(value: string)` - установка адреса карточки в каталоге.
`set imageAdd(value: string)` - установление описания изображения.
`set category(value: CategoryKey)` - установление категории товара.


### Класс CardPreview extends Component<ICardPreview>
Обеспечивает функционирование карточки предварительного просмотра.

Поля класса:
`imageElement: HTMLImageElement` - хранит элемент изображения.
`categoryElement: HTMLElement` - хранит категорию, к которой относится товар.
`deleteCartButton: HTMLButtonElement` - хранит кнопку добавления и удаления товара.
`descriptionElement: HTMLElement` -  хранит описание товара.
 
Методы класса: 
`set image(value: string)` - установление адреса изображения.
`set imageAdd(value: string)` - установление описания изображения.
`set category(value: string)` - установление категории для товара.
`set price(value: number | null)` - переопределяет метод родительского класса.
`set buttonChange(value: boolean)` - метод определяющий наличие товара в корзине.
`set description(value: string)` - установление описания товара.


### Класс CardBasket extends Component<ICardBasket>
Обеспечивает нахождение карточки в корзине.

Поля класса:
`indexElement: HTMLElement` - номер карточки в списке товаров корзины.
`deleteButton: HTMLButtonElement` - кнопка удалить карточку из списка в корзине.
 
Методы класса: 
`set index(value: number)` - определение номера карточки в списке корзины.


### Класс CardGeneral extends Component<ICardGeneral>
Класс описывает базовый функционал карточки товара.  

Поля класса:
`titleElement: HTMLElement` - название товара.
`priceElement: HTMLElement` - цена товара.
`protected ID: string = ''`- идентификатор товара.
 
Методы класса: 
`set id(value: string)` - установление идентификатора товара.
`set title(value: string)` - установление наименования товара.
`set cost(value: string | null)` - установление стоимости товара.


### Класс Basket extends Component<IBasket>
Класс описывает корзину товаров.

Поля класса:
`listElement: HTMLUListElement`- список товаров;
`buttonElement: HTMLButtonElement` - кнопка оформить покупку.
`totalElement: HTMLElement` - общая стоимость всех товаров.
 
Методы класса:
`set list(items: HTMLElement[])`- определение перечня товаров для покупки.
`set total(value: string)` - определение стоимости всех товаров для покупки.
`set button(state: boolean)` - устанавливает булевое значение для блокировки кнопки.


### Класс Form extends Component<IForm>
Образует родительский класс для классов Order и Contacts.

Поля класса:
`errElement: HTMLElement` - элемент с сообщением об ошибке.
`subButton: HTMLButtonElement` - кнопка отправки данных.
`formElement: HTMLFormElement` - элемент формы.
 
Методы класса: 
`showError(errors: string[]): void` - меняет отображение кнопки (заблокирована или нет).
`set error(value: string)` - установление текста ошибки.


### Класс Order extends Form<IOrder>
Класс описывает форму оформления заказа, которая требует введения адреса доставки и выбор способа оплаты.

Поля класса:
`cardButton: HTMLButtonElement` - кнопка онлайн оплаты;
`cashButton: HTMLButtonElement` - кнопка оплаты при получении;
`addressInput: HTMLInputElement` - поле для ввода адреса доставки;

Методы класса: 
`set address(value: string)` - установление адреса доставки.
`set paymentMethod(value: TPayment)` - меняет наличие класса button_alt-active у кнопки в зависимости от метода оплаты.

### Класс Contacts extends Form<IContacts>
Класс описывает форму оформления заказа, которая требует ввести номер телефона и адрес электронной почты. 

Поля класса:
`phoneInput: HTMLInputElement` - хранит адрес электронной почты.
`emaiInput: HTMLInputElement` - хранит номер телефона.
`submitButton: HTMLButtonElement` - кнопка для оплаты.

Методы класса: 
`set email(value: string)` - установление адреса электронной почты.
`set phone(value: string)` - установление номера телефона.


# События, вызываемые в приложении


## События модели данных

### События класса BasketModel
`basket:change` - событие генерируется при изменении данных корзины.

### События класса Customer
`order:updated` - событие генерируется при изменении данных о покупателе.

### События класса ProductCatalog
`productCatalog:products` - событие генерируется при добавлении товаров в каталог. 
`cardCatalog:openCard` - событие генерируется при выборе карточки для подробного просмотра.


## События слоя представления

### События класса Basket
`basket:makeOrder` - событие генерируется при попытке совершить заказ.

### События класса CardBasket
`basket:delete` - событие генерируется при попытке удалить карточку из корзины товаров.

### События класса CardCatalog
`cardCatalog:selected` - событие генерируется при попытке открыть полную карточку товара.

### События класса CardPreview
`cardPreviewButton:click` -  событие генерируется при попытке добавить товар в корзину или удалить его.
`cardPreviewButton:change` - событие генерируется при изменении внешнего вида кнопки добавления/удаления товара.

### События класса Contacts
`customer:change` - событие генерируется при попытке пользователя ввести данные о покупателе.
`contacts:submit` - событие генерируется при попытке отправки контактных данных формы.

### События класса Header
`basket:open` - событие генерируется при клике на иконку корзины, которое сообщает, что пользователь хочет открыть корзину.

### События класса Modal
`modal:close` - событие генерируется при попытке закрыть модальное окно при нажатии на кнопку закрытия, либо вне области модального окна.

### События класса Order
`customer:change` - событие генерируется при попытке пользователя ввести данные о покупателе.
`order:submit` - событие генерируется при попытке отправки данных формы об оплате.

### События класса Success
`modal:close` - событие генерируется при попытке закрыть модальное окно при нажатии на кнопку закрытия, либо вне области модального окна.



## Слой презентера (main.ts)

### function initialization()
Функция в которой происходят происходят подписки на события.

Подписки:

#### order:updated
При подписке на событие изменения данных покупателя 'order:updated' класса Customer происходит установление этих данных в поля форм создания заказа и передача в формы объекты с ошибками.

#### basket:change
При изменении данных в модели корзины происходит перерисовка компонентов по подписке на событие 'basket:change'. Изменяется список товаров, общая сумма, счетчик корзины и состояние кнопки оформления заказа.

#### basket:delete
При подписке на событие 'basket:delete' происходит удаление товара из списка корзины в окне с корзиной.

#### productCatalog:products
При подписке на событие отрисовки каталога товаров в галерее 'productCatalog:products' происходит получение массива товаров из данных модели каталога, для каждого элемента массива создается карточка товара и каталог рендерится из массива карточек товара.

#### getBasketList()
Для получения массива карточек товаров (контейнеров li) была выведена стрелочная функция const getBasketList = (): HTMLElement[]. Внутри происходит получение массива добавленных в корзину товаров, для каждого из которых создается экземпляр карточки товара в корзине. Функция возвращает массив карточек товара.

#### basket:open
Событие 'basket:open' отрисовывает корзину товаров в модальном окне.

#### basket:makeOrder
При подписке на событие 'basket:makeOrder' из корзины содержимое модального окна рендерится в компонент формы с полями способ оплаты и адрес, перед рендерингом происходит первичная валидация для проверки данных формы.

#### customer:change
Событие 'customer:change' фиксирует изменение данных покупателя и вызывает метод модели данных для обновления имеющихся данных на новые.

#### order:submit
При событии 'order:submit' модальное окно перерисовывается в новую форму создания заказа.

#### contacts:submit
При подписке на событие 'contacts:submit' вызывается валидация данных пользователя, если валидация пройдена, заказ отправляется на сервер. Если ответ с сервера успешный, то контент модального окна перерисовывается в компонент сообщения об удачном заказе. Данные пользователя и корзина товаров очищаются.

#### modal:close
При подписке на событие 'modal:close' вызывается метод модального окна close(), который закрывает модальное окно.